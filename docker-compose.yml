services:
  trajectory:
    build: ./trajectory
    ports:
      - "13000:13000/tcp"
      - "14000:14000/tcp"

  reconstruction:
    build: ./reconstruction
    ports:
      - "15000:15000/tcp"
    depends_on:
      - trajectory
