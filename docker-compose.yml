services:
  trajectory:
    build: ./trajectory
    image: enord-trajectory-service
    ports:
      - "13000:13000/tcp"
      - "14000:14000/tcp"

  reconstruction:
    build: ./reconstruction
    image: enord-reconstruction-service
    ports:
      - "15500:15500/tcp"
    depends_on:
      - trajectory
